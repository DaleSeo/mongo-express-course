<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Fake Users</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  </head>
  <body>
    <div class="container">
      <div class="page-header">
        <h1>User Detail</h1>
      </div>
      <div class="text-right">
        <a href="/users" class="btn btn-primary">
          <span class="glyphicon glyphicon-list"/>
        </a>
        <a href="<%=user._id%>/edit" class="btn btn-primary">
          <span class="glyphicon glyphicon-edit"/>
        </a>
        <button onclick="if (confirm('Are you sure?')) {location = '<%=user._id%>/del'}" type="button" class="btn btn-primary">
          <span class="glyphicon glyphicon-trash"/>
        </button>
      </div>
      <div class="media">
        <div class="media-left">
          <img class="media-object" src="/<%=user.photo.path%>" width="200"/>
        </div>
        <div class="media-body">
          <h3 class="media-heading">
            <%=user.name%>
          </h3>
          <br/>
          <dl>
            <dt>Role</dt>
            <dd><%=user.role%></dd>
            <dt>Email</dt>
            <dd><%=user.email%></dd>
            <dt>Score</dt>
            <dd><%=user.score%></dd>
        </dl>
        </div>
      </div>
    </div>
  </body>
</html>
